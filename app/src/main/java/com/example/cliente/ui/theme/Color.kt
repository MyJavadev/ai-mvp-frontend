package com.example.cliente.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de Colores Ritmo - Tonos Suaves y Pastel

// Colores Principales (Azules suaves)
val RitmoDarkBlue = Color(0xFF4A6FA5)  // Azul medio suave (más claro y menos saturado)
val RitmoDarkBlueDarker = Color(0xFF3D5A85)  // Variante más oscura pero suave
val RitmoDarkBlueLighter = Color(0xFF6B8FC9)  // Variante más clara azul pastel

// Colores de Acento (Amarillos/Dorados suaves)
val RitmoGold = Color(0xFFFDB750)  // Amarillo dorado suave (menos saturado)
val RitmoGoldDark = Color(0xFFE8A73D)  // Variante más oscura pero suave
val RitmoGoldLight = Color(0xFFFFE09B)  // Amarillo pastel muy suave

// Tonos Pastel Complementarios
val RitmoPastelBlue = Color(0xFFB8D4F1)  // Azul pastel muy claro
val RitmoPastelYellow = Color(0xFFFFF4D6)  // Amarillo pastel muy claro
val RitmoPastelGreen = Color(0xFFD4E8D4)  // Verde pastel suave

// Colores Secundarios (Claros)
val RitmoWhite = Color(0xFFFFFFFF)  // Blanco puro
val RitmoLightGray = Color(0xFFF8F9FA)  // Gris muy claro casi blanco
val RitmoGray = Color(0xFFDDE2E8)  // Gris medio suave
val RitmoDarkGray = Color(0xFF8C9AAD)  // Gris oscuro suave

// Colores de Estado (versiones suaves)
val RitmoSuccess = Color(0xFF81C784)  // Verde suave para éxito
val RitmoWarning = Color(0xFFFFB74D)  // Naranja suave para alertas
val RitmoError = Color(0xFFE57373)  // Rojo suave para errores
val RitmoInfo = Color(0xFF64B5F6)  // Azul claro suave para información

// Colores con transparencia para overlays y efectos
val RitmoDarkBlueAlpha80 = Color(0xCC4A6FA5)
val RitmoGoldAlpha20 = Color(0x33FDB750)
val RitmoGoldAlpha40 = Color(0x66FDB750)

// Gradientes (para uso con Brush)
val RitmoGradientStart = RitmoDarkBlue
val RitmoGradientEnd = RitmoDarkBlueLighter

// Colores antiguos (mantener compatibilidad temporal)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

val Primary = Color(0xFF6200EE)
val PrimaryVariant = Color(0xFF3700B3)
val Secondary = Color(0xFF03DAC6)
val SecondaryVariant = Color(0xFF018786)
val Background = Color(0xFFFFFBFE)
val Surface = Color(0xFFFFFFFF)
val Error = Color(0xFFB00020)

